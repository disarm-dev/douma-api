// Get country from req
// Get instance_config from cache or from remote
// Get geodata from cache or remote

const get_geodata = (country) => {
  const geodata = {}
  return geodata
}

const cache_file(data, filename) {

}



//


{
  "___disarm_area_id": "31"
}